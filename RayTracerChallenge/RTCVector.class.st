"
I am a an RTCVector, defined as an RTCTuple with a w of 0.0
"
Class {
	#name : #RTCVector,
	#superclass : #RTCTuple,
	#category : #RayTracerChallenge
}

{ #category : #initialization }
RTCVector >> initialize [

	w := 0.0.
]
